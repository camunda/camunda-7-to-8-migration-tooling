camunda:
  migrator:
    auto-ddl: true
    c7:
      data-source:
        auto-ddl: true
        jdbc-url: jdbc:h2:mem:migrator
        username: sa
        password: sa
        driver-class-name: org.h2.Driver
    c8:
      data-source:
        auto-ddl: true
        jdbc-url: jdbc:h2:mem:migrator
        username: sa
        password: sa
        driver-class-name: org.h2.Driver
  #process-test:
  #  camunda-docker-image-version: 8.9-SNAPSHOT

logging:
  level.io.camunda.migrator: INFO
  level.org.camunda.bpm.engine.impl.persistence.entity.HistoricProcessInstanceEntity: INFO

---

spring:
  config:
    activate:
      on-profile: history-level-full

camunda:
  migrator:
    auto-ddl: true
    c8:
      data-source:
        auto-ddl: true
        jdbc-url: jdbc:h2:mem:migrator
        username: sa
        password: sa
        driver-class-name: org.h2.Driver