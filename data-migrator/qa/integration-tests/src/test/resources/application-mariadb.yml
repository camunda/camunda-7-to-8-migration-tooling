camunda:
  migrator:
    auto-ddl: true
    c7:
      data-source:
        auto-ddl: true
        jdbc-url: jdbc:mariadb://localhost:33062/process-engine
        username: camunda
        password: camunda
        driver-class-name: org.mariadb.jdbc.Driver

---

spring:
  config:
    activate:
      on-profile: history-level-full

camunda:
  migrator:
    auto-ddl: true
    c8:
      data-source:
        auto-ddl: true
        jdbc-url: jdbc:mariadb://localhost:33062/process-engine
        username: camunda
        password: camunda
        driver-class-name: org.mariadb.jdbc.Driver
